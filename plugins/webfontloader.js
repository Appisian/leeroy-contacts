const WebFont = require("webfontloader"); //eslint-disable-line
WebFont.load({
  google: {
    families: ['Montserrat:400,500,600', 'Lato:300,300i,400,700'],
  },
  timeout: 1000, // Set the timeout to two seconds
});
